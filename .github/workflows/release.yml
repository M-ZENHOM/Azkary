name: Build and Release

on:
  push:
    branches:
      - master
      - main
  release:
    types: [published]

jobs:
  build-windows:
    runs-on: windows-latest
    timeout-minutes: 30

    steps:
      - uses: actions/checkout@v3

      - name: Set up Bun
        uses: oven-sh/setup-bun@v1
        with:
          bun-version: latest

      - name: Install dependencies
        run: |
          bun install --ignore-scripts
        env:
          npm_config_platform: win32
          npm_config_arch: x64

      - name: Build Windows
        run: bun run build-only && bunx electron-builder --win --publish always --config.compression=normal
        env:
          ELECTRON_BUILDER_ALLOW_UNRESOLVED_DEPENDENCIES: true

  build-macos:
    runs-on: macos-latest
    timeout-minutes: 30

    steps:
      - uses: actions/checkout@v3

      - name: Set up Bun
        uses: oven-sh/setup-bun@v1
        with:
          bun-version: latest

      - name: Install dependencies
        run: |
          bun install --ignore-scripts
        env:
          npm_config_platform: darwin
          npm_config_arch: x64

      - name: Build macOS
        run: bun run build-only && bunx electron-builder --mac --publish always --config.compression=normal
        env:
          ELECTRON_BUILDER_ALLOW_UNRESOLVED_DEPENDENCIES: true
